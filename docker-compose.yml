services:
  bank-categorizer:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - bank-categorizer-data:/app/data
    restart: unless-stopped

volumes:
  bank-categorizer-data:
